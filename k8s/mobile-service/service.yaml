apiVersion: v1
kind: Service
metadata:
  name: mobile-service
  namespace: microservices
  labels:
    app: mobile-service
spec:
  type: ClusterIP
  ports:
    - port: 8082
      targetPort: 8082
      protocol: TCP
      name: http
  selector:
    app: mobile-service
