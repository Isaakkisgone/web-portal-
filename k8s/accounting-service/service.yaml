apiVersion: v1
kind: Service
metadata:
  name: accounting-service
  namespace: microservices
  labels:
    app: accounting-service
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: 8081
      protocol: TCP
      name: http
  selector:
    app: accounting-service
